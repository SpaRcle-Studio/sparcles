---

---

<div class="download__card my-2 mx-2 bg-card min-w-64 w-full xl:max-w-[500px] p-5 flex flex-col rounded-xl">
    <slot />
</div>

<style>
    .download__card {
        background: rgba(var(--card), 0.7);
        border: 1px solid rgba(var(--border), 0.7);
        box-shadow: 0 12px 40px rgba(0, 0, 0, 0.35);
        border-radius: var(--radius);
    }
</style>
